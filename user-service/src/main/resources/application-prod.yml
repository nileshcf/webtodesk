spring:
  datasource:
    url: jdbc:mysql://prod-db-host:3306/userdb
    username: user_service
    password: ${USER_SERVICE_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
