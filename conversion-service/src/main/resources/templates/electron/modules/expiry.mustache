// Module: expiry — Tier: TRIAL
// Locks the app with a full-screen overlay once the trial expires.
// The lock screen is non-dismissible and shows an upgrade button.
//
// Config (from config.expiry):
//   expiresAt   — ISO-8601 string | null.  Null = never expires (module is a no-op).
//   lockMessage — string.  Message body on the lock screen.
//   upgradeUrl  — string.  URL opened when the user clicks "Upgrade Now".
//
// Implementation: preload-only (DOM overlay). No main-process side effects.
// Works on Windows, macOS, Linux.

var LOCK_ID          = '__wtd-expiry-lock';
var CHECK_INTERVAL   = 60000; // re-check every 60 s for real-time expiry

function setup() {}

function preloadSetup(contextBridge, ipcRenderer, config) {
  if (!config.modules || config.modules.indexOf('expiry') === -1) return;

  var exp         = config.expiry || {};
  var expiresAt   = exp.expiresAt   || null;
  var lockMessage = exp.lockMessage || 'Your trial has expired. Please upgrade to continue.';
  var upgradeUrl  = exp.upgradeUrl  || 'https://webtodesk.com/pricing';

  if (!expiresAt) return;

  var expiryTime = new Date(expiresAt).getTime();
  if (isNaN(expiryTime)) return;

  function isExpired() {
    return Date.now() >= expiryTime;
  }

  function showLockScreen() {
    if (document.getElementById(LOCK_ID)) return;

    // ── Outer overlay ─────────────────────────────────────────────────────
    var overlay = document.createElement('div');
    overlay.id  = LOCK_ID;
    overlay.style.cssText = [
      'position:fixed',
      'top:0', 'left:0',
      'width:100%', 'height:100%',
      'background:#0f0f11',
      'color:#f5f5f7',
      'z-index:2147483647',
      'display:flex',
      'flex-direction:column',
      'align-items:center',
      'justify-content:center',
      'font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif',
      'pointer-events:all',
      '-webkit-app-region:no-drag'
    ].join(';');

    // ── Lock icon ─────────────────────────────────────────────────────────
    var icon = document.createElement('div');
    icon.style.cssText = 'font-size:52px;margin-bottom:20px;line-height:1;';
    icon.textContent   = '\uD83D\uDD12';  // 🔒

    // ── Heading ───────────────────────────────────────────────────────────
    var heading = document.createElement('h2');
    heading.style.cssText = [
      'margin:0 0 12px',
      'font-size:22px',
      'font-weight:600',
      'letter-spacing:-0.3px'
    ].join(';');
    heading.textContent = 'Trial Expired';

    // ── Message ───────────────────────────────────────────────────────────
    var msg = document.createElement('p');
    msg.style.cssText = [
      'margin:0 0 30px',
      'font-size:15px',
      'color:#a0a0b0',
      'max-width:360px',
      'text-align:center',
      'line-height:1.6'
    ].join(';');
    msg.textContent = lockMessage;

    // ── Upgrade button ────────────────────────────────────────────────────
    var btn = document.createElement('button');
    btn.style.cssText = [
      'background:#ea580c',
      'color:#fff',
      'border:none',
      'padding:11px 32px',
      'border-radius:8px',
      'font-size:15px',
      'font-weight:600',
      'cursor:pointer',
      'letter-spacing:0.1px',
      'transition:opacity 0.15s'
    ].join(';');
    btn.textContent   = 'Upgrade Now \u2192';  // → arrow
    btn.onmouseover   = function() { btn.style.opacity = '0.85'; };
    btn.onmouseout    = function() { btn.style.opacity = '1'; };
    btn.onclick       = function() {
      try { ipcRenderer.send('open-external', upgradeUrl); } catch (_) {}
    };

    overlay.appendChild(icon);
    overlay.appendChild(heading);
    overlay.appendChild(msg);
    overlay.appendChild(btn);

    var root = document.body || document.documentElement;
    if (root) {
      root.appendChild(overlay);
      // Prevent scroll / pointer interaction on the underlying page
      if (document.body) {
        document.body.style.overflow = 'hidden';
        document.body.style.pointerEvents = 'none';
      }
    }
  }

  function checkAndLock() {
    if (isExpired()) showLockScreen();
  }

  // ── Initial check ─────────────────────────────────────────────────────────
  if (document.body) {
    checkAndLock();
  } else {
    window.addEventListener('DOMContentLoaded', checkAndLock);
  }

  // ── Periodic check (catches expiry during a long session) ─────────────────
  setInterval(checkAndLock, CHECK_INTERVAL);
}

function teardown() {
  try {
    var el = document.getElementById(LOCK_ID);
    if (el) el.remove();
  } catch (_) {}
}

module.exports = { setup, preloadSetup, teardown };
