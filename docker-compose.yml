version: '3.8'

services:
  webtodesk:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: webtodesk-app
    ports:
      - "7860:7860"
    env_file:
      - .env
    restart: unless-stopped